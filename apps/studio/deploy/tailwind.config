import type { Config } from 'tailwindcss';

const config: Config = {
  darkMode: 'class',
  content: [
    './src/**/*.{ts,tsx}',
    './components/**/*.{ts,tsx}',
    './app/**/*.{ts,tsx}',
    './pages/**/*.{ts,tsx}',
  ],
  theme: {
    extend: {
      colors: {
        neon: {
          DEFAULT: '#00ffff',
          secondary: '#ff00ff',
          tertiary: '#ffff00',
          success: '#00ff00',
          warning: '#ff9900',
          error: '#ff0000',
        },
        glass: {
          bg: 'rgba(20, 20, 30, 0.85)',
          border: 'rgba(80, 80, 95, 0.25)',
        },
      },
      boxShadow: {
        neon: '0 0 12px var(--neon-color, #00ffff)',
        glass: '0 8px 32px rgba(0, 0, 0, 0.15)',
      },
      backdropBlur: {
        sm: '4px',
        md: '12px',
        lg: '20px',
      },
    },
  },
  plugins: [],
};

export default config;